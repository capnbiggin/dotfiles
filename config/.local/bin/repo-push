#!/usr/bin/env bash

pushd ~/Documents/My-Brain
git add .
git commit -m "automatic commit"
git push
popd

pushd ~/dotfiles
git add .
git commit -m "automatic commit"
git push
popd

pushd ~/.local/share/capndot
git add .
git commit -m "automatic commit"
git push
popd
