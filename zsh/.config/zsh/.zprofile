
# run Hyprland on login from z shell
#if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
#hyprland
#fi

# run startx on login from z shell
#if [[ -z $DISPLAY ]] && [[ $(tty) = /dev/tty1 ]]; then
#startx
#fi
